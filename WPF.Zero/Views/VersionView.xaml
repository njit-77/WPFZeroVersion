<Window x:Class="WPF.Zero.Views.VersionView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF.Zero.Views"
        mc:Ignorable="d"
        Title="关于 WPF.Zero" Height="200" Width="450" 
        ResizeMode="NoResize" 
        WindowStartupLocation="CenterScreen" 
        Icon="/Resources/Image/About.png">

    <Window.Resources>
        <Style TargetType="{x:Type TextBlock}">
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <DockPanel>
            <Image Source="/Resources/ico/software.ico"/>
            <TextBlock Text="WPF.Zero" FontFamily="Cascadia Code" FontSize="21"/>
        </DockPanel>
        <TextBlock Grid.Row="1" FontFamily="Cascadia Code" x:Name="VersionText"/>
        <Button Grid.Row="2"
                Content="关闭"
                Margin="10,5"
                Width="70"
                Height="25"
                HorizontalContentAlignment="Center"
                HorizontalAlignment="Right"
                x:Name="Close"/>
    </Grid>
</Window>
